({
	doInitClasses : function(component, event, helper) {
		helper.initClasses(component);
	},
    toggleTypeDropdown : function(component, event, helper) {
        helper.handleToggleTypeDropdown(component);
    },
    selectMenuType : function(component, event, helper) {
        helper.handleToggleTypeDropdown(component);
    }
})